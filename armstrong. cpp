#include<iostream>

using namespace std;
int main()
{
 long long n,num,sum=0;
 cin>>n;
 num=n;
 while(n)
 {
  long long x=n%10;
  n/=10;
  x=x*x*x;
  sum+=x;
 }
 if(sum==num)
  cout<<"It's is a Armstrong Number\n";
 else
  cout<<"It's is a not Armstrong Number\n";
}
